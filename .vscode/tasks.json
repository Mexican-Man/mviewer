{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build cmd",
            "type": "shell",
            "command": "go build -o ${workspaceFolder}/bin/mviewer",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "detail": "Compile the tool.",
            "group": "build",
            "problemMatcher": [
                "$go"
            ]
        }
    ]
}